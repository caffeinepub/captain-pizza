// This component is no longer used in the routed app structure
// Checkout functionality has been moved to CheckoutPage.tsx
// Kept for backwards compatibility but not rendered in the new routing structure
export function OrderSection() {
  return null;
}
